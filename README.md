# CoreJS

Migración de Core (PHP) a JavaScript/TypeScript separando frontend y backend comunicados por API REST.

- Legacy (referencia): `legacy/`
- Backend: `apps/backend` (NestJS)
- Frontend: `apps/frontend` (Vue 3 + Vite)

